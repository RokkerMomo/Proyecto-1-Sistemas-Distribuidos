const express = require('express');

const router = express.Router();

router.get('/',(req,res)=>{
    
// //Obtener Productos
// product.getProducts({}, (err, res) => {
//     if (!err) {
//         console.log(res);
//         }
//         });

})

module.exports = router;